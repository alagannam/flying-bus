import { ComingSoon } from '@/components/ui/ComingSoon'
import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Roles' }

export default function Page() {
  return <ComingSoon title="Roles" phase={8} />
}
